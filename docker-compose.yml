services:
  db:
    image: postgres:16-alpine
    container_name: ms-agenda-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "${POSTGRES_PORT}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
    volumes:
      - postgres_agenda_data:/var/lib/postgresql/data # Nombre de volumen único
    networks:
      - ms-agenda-net

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ms-agenda-api
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    environment:
      NODE_ENV: development
      PORT: ${PORT}
      # Cambiado de '@db:5432' a '@ms-agenda-db:5432' por seguridad DNS
      DATABASE_URL: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@ms-agenda-db:5432/${POSTGRES_DB}?schema=public
      MS_ENTIDADES_URL: ${MS_ENTIDADES_CORE_URL}
    ports:
      - "${PORT}:${PORT}"
    networks:
      - ms-agenda-net

volumes:
  postgres_agenda_data: # Nombre de volumen único para evitar colisiones locales

networks:
  ms-agenda-net:
    driver: bridge